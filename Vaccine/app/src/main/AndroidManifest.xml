<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vaccine">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Vaccine">
        <activity android:name=".Update"></activity>
        <activity android:name=".DetailActivity" />
        <activity android:name=".showdata" />
        <activity android:name=".HomeIS" />
        <activity android:name=".successregist" />
        <activity android:name=".successform" />
        <activity android:name=".DetailForm" />
        <activity android:name=".Home" />
        <activity android:name=".SignupPage" />
        <activity android:name=".LoginPage" />
        <activity android:name=".onboarding" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>